---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: components-workload-clusters
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: https://github.com/nocturnalastro/vse-carslab-managed
        revision: volante-pipeline
        directories:
          - path: clusters/overlays/*
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 15
          backoff:
            duration: 15s
            factor: 2
            maxDuration: 5m
      source:
        repoURL: https://github.com/nocturnalastro/vse-carslab-managed
        targetRevision: volante-pipeline
        path: '{{path}}'
      destination:
        server: https://api.volante.cars.lab:6443
